# Architecture Review Result

**Date:** 2026-03-30
**Project:** senko
**Mode:** flat

## Summary

- 🔴 Critical: 16件
- 🟠 High: 26件
- 🟡 Medium: 20件
- 🔵 Low: 9件

---

## Findings

### 🔴 Critical

#### [smart-repository] src/domain/repository.rs:23
TaskRepository に list_tasks(filter) が定義されている。ListTasksFilter はステータス・タグ・依存関係・ready フラグなどのビジネス条件を含む複合フィルタであり、スマートリポジトリアンチパターンに該当する。

**Suggestion:** list_tasks は独立した TaskQuery / TaskQueryPort インターフェースとして分離し、TaskRepository は get_task / save / delete 程度に限定する。

---

#### [smart-repository] src/domain/repository.rs:24
next_task(project_id) は「次に実行すべきタスクを取得する」という優先度・ステータスに基づいたビジネスロジックをリポジトリインターフェースに組み込んでいる。

**Suggestion:** 独立した TaskSelector または NextTaskQuery インターフェースに切り出す。

---

#### [smart-repository] src/domain/repository.rs:25
task_stats(project_id) はタスクの集計統計を返すビジネスクエリであり、リポジトリの責務を超えている。

**Suggestion:** TaskStatsQuery などの独立したインターフェースとしてドメイン層または application 層に定義する。

---

#### [smart-repository] src/domain/repository.rs:26
ready_count(project_id) は「ready 状態のタスク数」というビジネス集計をリポジトリに持ち込んでいる。

**Suggestion:** task_stats と同様に集計クエリ用の独立インターフェースへ移動する。

---

#### [smart-repository] src/domain/repository.rs:27
list_ready_tasks(project_id) は「ready 状態のタスク一覧を取得する」というステータスフィルタ付きの業務クエリであり、スマートリポジトリアンチパターンに該当する。

**Suggestion:** list_tasks から派生した独立クエリインターフェースへ切り出す。

---

#### [domain-knowledge-leak] src/infra/sqlite/mod.rs:505
delete_project() がインフラ層でビジネスルールを実装している。id==1 の削除を禁止する制約と、タスクが存在するプロジェクトを削除できないという制約はドメインルールであり、リポジトリ実装に直接書かれている。

**Suggestion:** ドメイン層に Project エンティティの delete() メソッドを定義し、そこで削除可能かどうかを判断する。インフラ層は純粋な DELETE のみを実行する。

---

#### [domain-knowledge-leak] src/infra/postgres/mod.rs:282
PostgresBackend::delete_project() でも同様に id==1 の削除禁止ルールとタスク存在チェックがインフラ層に実装されている。

**Suggestion:** SQLite 版と同じく、ビジネスルールをアプリケーション層またはドメイン層に移動する。

---

#### [domain-knowledge-leak] src/infra/dynamodb/mod.rs:655
DynamoDbBackend::delete_project() でも id==1 の削除禁止ルールがインフラ層に実装されている。同一のビジネスルールが 3 か所に重複。

**Suggestion:** ビジネスルールを一箇所（ドメイン層またはアプリケーション層）に集約する。

---

#### [domain-knowledge-leak] src/infra/sqlite/mod.rs:1186
list_tasks() の SQL クエリ内で filter.ready フラグを処理する際に「依存タスクが全て completed であれば ready」という定義が SQL にハードコードされている。

**Suggestion:** ready 判定ロジックをドメイン層の Task::is_ready() 等に定義する。パフォーマンス上 SQL 最適化が必要な場合は意図的な例外として管理する。

---

#### [domain-knowledge-leak] src/infra/postgres/mod.rs:999
PostgresBackend::list_tasks() でも同様に ready フィルタのロジックが SQL クエリに直接埋め込まれている。

**Suggestion:** SQLite 版と同様に、ready 判定をドメイン層に移動する。

---

#### [domain-knowledge-leak] src/infra/sqlite/mod.rs:1216
next_task() の SQL クエリで「status = 'todo' かつ依存タスクが全て completed」という条件と「priority ASC, created_at ASC, id ASC」という優先度ソート順をハードコードしている。

**Suggestion:** タスクの優先度選択アルゴリズムをドメイン層（例: TaskSelectionPolicy）に定義し、リポジトリは ready なタスクの一覧を返すだけにする。

---

#### [domain-knowledge-leak] src/infra/postgres/mod.rs:1034
PostgresBackend::next_task() でも同一の優先度ソートルールがハードコードされた SQL として実装されている。

**Suggestion:** SQLite 版と同様の対処。

---

#### [domain-knowledge-leak] src/infra/dynamodb/mod.rs:941
DynamoDbBackend::next_task() でも同一のタスク選択ロジックがアプリケーションコードとして実装されている。3 つのバックエンドで同一のビジネスルールが独自実装されている。

**Suggestion:** タスク選択ロジックを共通のドメイン層メソッドに移動する。

---

#### [fat-controller] src/presentation/cli/handlers.rs:183
cmd_list が TaskService を経由せず backend.list_tasks() を直接呼び出している。アプリケーション層をバイパスしている。

**Suggestion:** task_service.list_tasks() を呼び出すよう修正する。

---

#### [fat-controller] src/presentation/cli/handlers.rs:201
cmd_get が TaskService を経由せず backend.get_task() を直接呼び出している。

**Suggestion:** task_service.get_task() を呼び出すよう修正する。

---

#### [fat-controller] src/presentation/cli/handlers.rs:1157
cmd_edit の非 dry-run パスで backend.update_task()、backend.update_task_arrays()、backend.get_task() を直接呼び出している。api.rs の edit_task ハンドラと矛盾しており、フック発火等のビジネスロジックが CLI では実行されない。

**Suggestion:** task_service.edit_task() および task_service.edit_task_arrays() を呼び出すよう修正する。

---

### 🟠 High

#### [smart-repository] src/domain/repository.rs:36
ready_task / start_task / complete_task / cancel_task / check_dod / uncheck_dod はリポジトリインターフェースにデフォルト実装として状態遷移ロジック（ビジネスルール）を埋め込んでいる。

**Suggestion:** これらのメソッドをドメインサービスまたはアプリケーションサービスへ移動する。

---

#### [smart-repository] src/domain/repository.rs:97
ProjectRepository がプロジェクト管理・ユーザー管理・プロジェクトメンバーシップ・API キー管理を単一インターフェースに束ねている。

**Suggestion:** ProjectRepository / UserRepository / ApiKeyRepository に分割する。

---

#### [smart-repository] src/domain/repository.rs:101
list_projects() および list_users() はフィルタ・ページネーションなしの全件取得がリポジトリ契約として固定化されている。

**Suggestion:** ProjectQueryPort / UserQueryPort のような独立クエリインターフェースを用意するか、ページネーション対応に変更する。

---

#### [smart-repository] src/domain/repository.rs:28
add_dependency / remove_dependency / set_dependencies / list_dependencies はタスク依存関係の操作をリポジトリのメソッドとして定義している。

**Suggestion:** Task 集約のメソッドを呼んだ後に TaskRepository.save() するパターンに統一する。

---

#### [dependency-direction] src/presentation/api.rs:15
presentation layer が infrastructure 層の具体実装 GhCliPrVerifier を直接インポートしている。

**Suggestion:** bootstrap.rs 側で pr_verifier の構築を行い、presentation には Arc<dyn PrVerifier> を受け取るインターフェースにする。

---

#### [dependency-direction] src/presentation/api.rs:22
presentation layer が infrastructure 層の crate::infra::hook モジュールを直接インポートしている。

**Suggestion:** 設定読み込み関数を bootstrap モジュールへ移動する。

---

#### [dependency-direction] src/presentation/api.rs:23
presentation layer が crate::infra::hook::RuntimeMode を直接参照している。

**Suggestion:** RuntimeMode を application::port または bootstrap に移動する。

---

#### [dependency-direction] src/presentation/api.rs:24
presentation layer が crate::infra::hook::executor::ShellHookExecutor を直接インスタンス化している。

**Suggestion:** ShellHookExecutor の生成は bootstrap に委譲する。

---

#### [dependency-direction] src/presentation/cli/mod.rs:11
presentation layer が crate::infra::hook を直接インポートしている。

**Suggestion:** config 読み込みロジックを bootstrap モジュールへ集約する。

---

#### [dependency-direction] src/presentation/cli/mod.rs:14
presentation layer が crate::infra::project_root::resolve_project_root を直接インポートしている。

**Suggestion:** resolve_project_root を bootstrap 経由で利用する。

---

#### [dependency-direction] src/presentation/cli/handlers.rs:17
presentation layer が crate::infra::hook を直接インポートしている。

**Suggestion:** bootstrap を経由させる。

---

#### [dependency-direction] src/presentation/cli/handlers.rs:24
presentation layer が crate::infra::project_root::resolve_project_root を直接インポートしている。

**Suggestion:** bootstrap 経由で提供する。

---

#### [dependency-direction] src/presentation/cli/skill.rs:7
presentation layer が crate::infra::project_root::resolve_project_root を直接インポートしている。

**Suggestion:** bootstrap 経由での利用に切り替える。

---

#### [business-logic-leak] src/application/task_service.rs:49
ブランチテンプレート展開ロジック（`${task_id}` の検出と置換）がアプリケーション層に実装されている。ドメインルールである。

**Suggestion:** Task ドメインエンティティまたは CreateTaskParams にテンプレート展開メソッドを追加し、ドメイン層に移動する。

---

#### [business-logic-leak] src/application/task_service.rs:192
complete_task 内で CompletionMode::PrThenComplete の条件チェックと pr_url の必須バリデーションがアプリケーション層に直接記述されている。

**Suggestion:** ドメイン層に CompletionPolicy のようなドメインサービス/値オブジェクトを導入する。

---

#### [domain-knowledge-leak] src/domain/repository.rs:36
TaskRepository トレイトにデフォルト実装として状態遷移ロジックが含まれており、ポート定義にビジネスロジックが混入。Utc::now() をリポジトリが呼び出すことで時刻取得の副作用もポートに混入。

**Suggestion:** デフォルト実装をアプリケーション層のサービスに移動する。

---

#### [domain-knowledge-leak] src/infra/postgres/mod.rs:531
get_user_by_api_key() 内で API キーをハッシュ化する処理がインフラ層に実装されている。SQLite 側との設計不統一。

**Suggestion:** 全バックエンドで一貫した設計にし、ハッシュ化は呼び出し側で行う。

---

#### [domain-knowledge-leak] src/infra/sqlite/mod.rs:1433
sync_config_defaults() で「デフォルトプロジェクトの id は常に 1」というビジネスルールがインフラ層に埋め込まれている。

**Suggestion:** デフォルトプロジェクト/ユーザーの概念をドメイン定数またはアプリケーションサービスで管理する。

---

#### [domain-knowledge-leak] src/infra/dynamodb/mod.rs:256
get_ready_tasks() 内部で ready 判定ロジックをインフラ層で実装している。

**Suggestion:** ready 判定をドメイン層の Task メソッドに集約する。

---

#### [fat-controller] src/presentation/cli/handlers.rs:390
cmd_next の using_http ブランチで hook_executor.fire() を直接呼び出している。フック発火はアプリケーション層の責務。

**Suggestion:** HTTP バックエンド向けの next_task フロー全体を TaskService 内に吸収する。

---

#### [fat-controller] src/presentation/cli/handlers.rs:286
cmd_ready の dry-run パスで task.status().transition_to() というドメインメソッドをハンドラが直接呼び出している。cmd_start (328) と cmd_cancel (465) にも同様のパターン。

**Suggestion:** dry-run 向けの状態遷移チェックは TaskService 側に validate_transition メソッドとして切り出す。

---

#### [fat-controller] src/presentation/web.rs:98
index_handler および graph_handler が AppState.backend を直接呼び出している。アプリケーションサービスを経由していない。

**Suggestion:** web.rs でも TaskService を AppState に持ち、backend を直接呼ぶ代わりに task_service 経由にする。

---

#### [fat-controller] src/presentation/web.rs:120
task_handler が state.backend.get_task(1, id) を直接呼び出し、project_id がハードコード。エラー時は一律 NOT_FOUND にマッピング。

**Suggestion:** TaskService 経由で呼び出し、エラー種別に応じて適切な HTTP ステータスに変換する。

---

#### [poor-error-handling] src/presentation/api.rs:138
classify_error() がエラーメッセージの文字列を contains() でマッチングしてエラー種別を判定している。文字列マッチングは脆弱。

**Suggestion:** ドメイン層で専用のエラー型（enum）を定義し、パターンマッチで分類する。

---

#### [poor-error-handling] src/presentation/api.rs:138
classify_error() の Internal ブランチで anyhow::Error の .to_string() をそのまま HTTP レスポンスに格納。内部エラー詳細がクライアントに露出する可能性。

**Suggestion:** Internal の場合はジェネリックなメッセージを返し、詳細はサーバーログのみに記録する。

---

#### [domain-exposure] src/presentation/api.rs:443
list_projects, get_project, list_users 等のハンドラがドメイン型をそのまま JSON シリアライズして返している。ドメインの内部構造が API 契約に直接露出。

**Suggestion:** API 応答用の専用 DTO（Response 型）を定義し、ドメインオブジェクトから変換してから返す。

---

### 🟡 Medium

#### [misplaced-code] src/domain/user.rs:4
domain 層のエンティティが clap::ValueEnum を derive している。CLI フレームワーク依存がドメインに混入。

**Suggestion:** ValueEnum derive を presentation/cli 層のラッパー型に移動する。

---

#### [misplaced-code] src/domain/task.rs:4
domain 層の Task/Priority 等のエンティティが clap::ValueEnum を derive している。

**Suggestion:** ValueEnum の derive は presentation/cli 層のラッパー型に移動する。

---

#### [misplaced-code] src/bootstrap.rs:1
bootstrap.rs は src/ のルートに置かれており、どのレイヤーにも属していない。composition root の位置づけが曖昧。

**Suggestion:** src/bootstrap/ として独立モジュールにするか、infrastructure 層に composition_root として配置する。

---

#### [misplaced-code] src/auth.rs:1
auth.rs は src/ のルートに置かれており、trait 定義・実装・axum extractor が混在している。

**Suggestion:** AuthProvider trait は application/port へ、ApiKeyProvider 実装は infra へ、axum extractor は presentation/api へ分離する。

---

#### [domain-event-naming] src/domain/task.rs:9
DodChecked / DodUnchecked は「状態」であり「起きた出来事」の表現として弱い。

**Suggestion:** DodItemChecked / DodItemUnchecked などより明確な命名に変更を検討する。

---

#### [domain-model-concern-mixing] src/domain/task.rs:24
HookTrigger はフック設定キー名への変換ロジックを持っており、インフラ・設定層の関心事をドメイン層に持ち込んでいる。

**Suggestion:** HookTrigger を application 層または infrastructure 層へ移動する。

---

#### [domain-layer-scope] src/domain/config/mod.rs:1
Config / HooksConfig / BackendConfig 等のインフラ・運用設定を表す型がドメイン層に配置されている。

**Suggestion:** 設定関連の型は src/config/ または src/infrastructure/config/ に移動する。

---

#### [domain-model-concern-mixing] src/domain/task.rs:673
DodItem のインデックス検証エラーが anyhow::Error に依存しており、ドメインエラー型（enum）ではない。

**Suggestion:** 専用の DomainError enum を定義する。

---

#### [business-logic-leak] src/application/task_service.rs:209
compute_unblocked 関数がアプリケーション層に実装されている。ドメインイベントの一部であるべき。

**Suggestion:** ドメインサービスに移動するか、ドメインイベントとして表現する。

---

#### [misplaced-port] src/application/port/hook_executor.rs:3
HookExecutor::fire のシグネチャに backend: &dyn TaskBackend が含まれており、ポートがリポジトリの実行時参照に依存。

**Suggestion:** fire から backend パラメータを取り除き、必要なデータは HookTrigger またはコンテキスト構造体に含める。

---

#### [missing-auth] src/application/project_service.rs:49
add_project_member、remove_project_member、update_member_role に権限チェックが存在しない。

**Suggestion:** 呼び出し元ユーザーのロールを検証する認可チェックを追加する。

---

#### [missing-auth] src/application/project_service.rs:36
delete_project に権限チェックが存在しない。Owner のみが実行可能な破壊的操作であるべき。

**Suggestion:** 削除前に Owner であることを確認する認可チェックを追加する。

---

#### [domain-knowledge-leak] src/infra/postgres/mod.rs:50
hash_api_key() が PostgresBackend 側にのみ存在し、バックエンド間でハッシュ処理の責務の置き場所が統一されていない。

**Suggestion:** get_user_by_api_key のシグネチャを key_hash: &str（ハッシュ済み）で統一する。

---

#### [missing-adapter] src/infra/http/mod.rs:341
get_user_by_api_key() が bail! で実装されており、ポートのコントラクトを満たしていない。

**Suggestion:** 未サポート操作を表現するエラー型（UnsupportedOperation）を用意する。

---

#### [missing-adapter] src/infra/dynamodb/mod.rs:791
DynamoDbBackend の API キー管理メソッドが全て bail! で「未実装」を返している。

**Suggestion:** API キー管理を別のオプショナルトレイトに分離する。

---

#### [domain-knowledge-leak] src/infra/hook/mod.rs:498
DEFAULT_PROJECT_ID = 1 と DEFAULT_USER_ID = 1 がインフラ層に定義されている。

**Suggestion:** ドメイン層またはアプリケーション層で一元管理する。

---

#### [domain-knowledge-leak] src/infra/hook/mod.rs:196
load_config() が設定ファイルの探索順序というアプリケーションレベルのロジックを含んでいる。

**Suggestion:** 設定ロード処理を bootstrap.rs または専用の設定サービスに移動する。

---

#### [missing-validation] src/presentation/cli/handlers.rs:76
cmd_add で --from-json / --from-json-file 使用時に title が空文字列であるケース等のバリデーションが行われていない。

**Suggestion:** デシリアライズ後に最低限の構文的バリデーションを行う。

---

#### [poor-error-handling] src/presentation/web.rs:97
バックエンドエラーを一律 INTERNAL_SERVER_ERROR にマッピングしており、エラーページなし。

**Suggestion:** ユーザー向けのエラーページを返し、詳細はログにのみ出力する。

---

#### [fat-controller] src/presentation/web.rs:97
index_handler と graph_handler でタグ一覧取得のために全タスクを再度取得する処理がハンドラ内に直接記述。

**Suggestion:** 専用のクエリメソッドをアプリケーション層に定義する。

---

### 🔵 Low

#### [dependency-direction] src/presentation/cli/handlers.rs:1571
テストコード内で crate::infra::sqlite::SqliteBackend を直接インスタンス化している。

**Suggestion:** テスト用ヘルパーを infra/bootstrap に置くか、in-memory mock を用意する。

---

#### [domain-model-concern-mixing] src/domain/user.rs:222
NewApiKey::generate() が UUID 生成・SHA-256 ハッシュ計算という実装詳細をドメインモデル内に持っている。

**Suggestion:** キー生成・ハッシュ計算をファクトリに分離する。

---

#### [domain-layer-scope] src/domain/task.rs:4
clap::ValueEnum が domain 型に直接 derive されている（重複指摘、medium の misplaced-code と同一観点）。

**Suggestion:** CLI 層のアダプタ型で対応する。

---

#### [business-logic-leak] src/application/task_service.rs:425
expand_branch_template 関数がアプリケーション層に定義されている。ブランチ命名規則はドメイン知識。

**Suggestion:** domain::task モジュールに移動する。

---

#### [business-logic-leak] src/application/user_service.rs:46
create_api_key 内で NewApiKey::generate() を呼び出している。現状は許容範囲だが、キー生成ポリシーが複雑化する場合に注意。

**Suggestion:** 現状維持。複雑化する場合はドメインサービスとして切り出す。

---

#### [configuration] src/infra/sqlite/mod.rs:241
xdg_data_base() が環境変数を直接読み込んでおり、XDG パス解決が複数箇所に散在。

**Suggestion:** XDG ディレクトリ解決を共通ユーティリティに集約する。

---

#### [configuration] src/infra/http/mod.rs:24
HttpBackend::new() でタイムアウトを 30 秒にハードコード。

**Suggestion:** タイムアウト値を設定から注入できるようにする。

---

#### [fat-controller] src/presentation/api.rs:747
get_config ハンドラが hooks::load_config() を直接呼び出している。

**Suggestion:** 設定を AppState に保持するか、アプリケーションサービスメソッドを用意する。

---

#### [fat-controller] src/presentation/web.rs:97
project_id がハードコードの 1 になっている。

**Suggestion:** デフォルトプロジェクト ID を設定から読み取るか AppState 経由で解決する。
